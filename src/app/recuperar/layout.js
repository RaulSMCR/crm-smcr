// /recuperar es "use client" y no puede exportar `metadata`.
export const metadata = {
  title: "Recuperar acceso",
  robots: { index: false, follow: false },
};

export default function RecuperarLayout({ children }) {
  return children;
}
